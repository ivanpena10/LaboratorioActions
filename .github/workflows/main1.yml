name: env_tutorial

on: [pull_request, push]
jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps: 
      - name: step1
        env: 
          API_KEY: Q452RY76512WW
        run: echo " The API key is:${{env.API_KEY}}"
      - name: step2 
        run: echo "The API key is:$API_KEY"
  job2: 
    runs-on: ubuntu-latest
    steps: 
      - name: step1
      
        run: echo "The API key is:$API_KEY"
        
